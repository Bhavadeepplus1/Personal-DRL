({
	/* getloginDetails:function(component, event, helper) {
        var getLoginDetails = component.get("c.getLoginDetailsUpdate");
        getLoginDetails.setParams({
           
        });
        getLoginDetails.setCallback(this, function (response) {
             var status = response.getState();
              if (status === 'SUCCESS') {
                   console.log("updated");
              }else {
                console.log('----error in home---' + response.getError());
            }
        });
           $A.enqueueAction(getLoginDetails);
        
    }*/
})